mixin logo(index=false)
  - const txt = index ? "Обновить страницу" : "На главную"

  a.logo(href="index.html")
    span.visually-hidden!= txt
    +picture(
      "logo",
      "logo",
      "png",
      "101",
      "30",
      "Логотип.",
    )
