mixin simple-intro(breadcrumbs, title)

  section.simple-intro&attributes(attributes)
    .simple-intro__light
    .container
      .simple-intro__grid
        h1.title.title--h1!= title
        +breadcrumbs(breadcrumbs)
